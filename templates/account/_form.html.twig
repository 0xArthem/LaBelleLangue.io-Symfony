{{ form_start(form) }}

<div class="row">
	<div class="col">
		{{ form_row(form.pseudo) }}
	</div>
	<div class="col">
		{{ form_row(form.email) }}
	</div>
</div>

{{ form_row(form.password) }}
{{ form_row(form.submit) }}

{{ form_end(form) }}
